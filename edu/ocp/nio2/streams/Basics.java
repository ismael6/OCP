package edu.ocp.nio2.streams;

/**
 * @author IsmaSp6
 *
 *	walking/traversing a directory.- iterate over all descendants of a parent until some condition is met
 *	
 *	depth-first search.- traverses from root to an arbitrary leaf, 
 *						then navigates back up toward the root 
 *						traversing fully down any paths skipped along the way
 *
 *		search depth.- distance from root to the current node
 *		maximum search depth.- limit of deep search levels (Integer.MAX_VALUE streams default )
 *
 *	breadth-first.- traverses all elements in the same depth before proceeding next depth
 *
 */
public class Basics {
	public static void main(String[] args) {
		
	}
}
