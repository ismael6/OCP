package edu.ocp.genericsAndCollections.listsSetsMApsQueues;

/**
 * @author IsmaSp6
 * 
 * List -> duplicates allowed, ordered by index
 * Map -> key-value pairs, duplicates values allowed but unique keys, unordered
 * Queue -> duplicates allowed, ordered by data structure nature (FIFO, LIFO)
 * Set -> NOT allows duplicates, unordered
 *
 */
public class CollectionInterfacesResume {}
