package edu.ocp.genericsAndCollections.listsSetsMApsQueues.list;

import java.util.ArrayList;

/**
 * @author IsmaSp6
 * List backed by an array 
 * "Resizable array" 
 * 	  when array is full, uses another with double capacity(arrays size don´t change)
 * 	
 *  get by index -> O(1)
 * 	add element -> O(n)
 *  remove element by iterator -> O(n)
 * 
 *  When to use: ordered elements, access by index, read operations >= write operations
 */
public class ArrayListImpl {
	public static void main(String[] args) {
		ArrayList arrayList;
	}
}
